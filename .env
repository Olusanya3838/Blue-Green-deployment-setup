# Blue/Green Deployment Configuration
# The CI/grader will set ALL these values

# Pre-built container images (we don't build these, just pull and run them)
BLUE_IMAGE=olusanya3838/nodejs-app:blue
GREEN_IMAGE=olusanya3838/nodejs-app:green

# Which pool is active: "blue" or "green"
# This tells Nginx which service is PRIMARY vs BACKUP
ACTIVE_POOL=blue

# Release IDs - we PASS these to the running containers
# The app reads these and returns them in X-Release-Id header
RELEASE_ID_BLUE=v1.0.0-blue
RELEASE_ID_GREEN=v1.0.0-green

# Port the Node.js app listens on inside the container
PORT=3000
